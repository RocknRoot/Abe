<%= form_for @comment, :url => comments_path do |f| %>

  <%= f.hidden_field :term, :value => @term.id %>
  <div class="commmment_form_content"><%= f.text_area "content" %></div>
  <div class="comment_form_submit"><%= f.submit I18n.t("comments.create") %></div>

<% end %>
