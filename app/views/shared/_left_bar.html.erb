<div id="left_bar">
  <% @categories.each do |c| %>
    <%= render "category/list", :locals => { :category => c } %>
  <% end %>
  <p><%= link_to t(:category_create_root), new_category_path %></p>
</div>
