<div id="left_bar">
  <% @categories.each do |c| %>
    <%= render "category/list", :locals => { :category => c } %>
  <% end %>
  <p><%= link_to "Create root category", new_category_path %></p>
</div>
